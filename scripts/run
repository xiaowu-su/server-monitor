#!/bin/bash
# 清空终端日志并执行命令，过滤apt警告
> /tmp/terminal_output.log
echo ">>> $@" >> /tmp/terminal_output.log
"$@" 2>&1 | grep -v "WARNING: apt does not have a stable CLI interface" | tee -a /tmp/terminal_output.log
